import React from 'react';
import {Box} from '@mui/material';
import {Header, Content, Footer} from './';

export const Main = () => (
    <Box>
        <Header />
        <Content />
        <Footer />
    </Box>
)

